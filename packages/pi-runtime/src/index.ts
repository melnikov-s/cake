/**
 * Sole application boundary for Pi's coding-agent SDK.
 *
 * The concrete AgentRuntime adapter will be added in the next S0 slice after
 * contract tests pin the public APIs exercised by Cake.
 */
export const piRuntimeVersion = "0.84.0" as const;
